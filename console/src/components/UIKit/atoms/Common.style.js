import styled from 'styled-components';
import {
  color,
  border,
  typography,
  layout,
  space,
  shadow,
} from 'styled-system';

export const BaseStyledDiv = styled.div`
  ${color}
  ${border}
  ${typography}
  ${layout}
  ${space}
  ${shadow}
`;
