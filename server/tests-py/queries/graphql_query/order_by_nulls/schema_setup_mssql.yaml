type: bulk
args:

- type: mssql_run_sql
  args:
    source: mssql
    sql: |
      CREATE TABLE order_by_nulls_test(
        id INT NOT NULL PRIMARY KEY,
        val INT
      );

      INSERT INTO order_by_nulls_test (id, val) VALUES
        (1, 3),
        (2, NULL),
        (3, 1),
        (4, NULL),
        (5, 2);
