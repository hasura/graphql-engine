module Language.GraphQL.Draft.Syntax
  ( ExecutableDocument,
    SchemaDocument,
  )
where

-------------------------------------------------------------------------------

type role ExecutableDocument nominal

data ExecutableDocument var

data SchemaDocument
