apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: hasura
    hasuraService: custom
  name: hasura
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: hasura
  type: ClusterIP
status:
  loadBalancer: {}
