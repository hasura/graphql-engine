---
sidebar_label: hasura scripts
sidebar_position: 33
description: Execute helper scripts to manage Hasura Projects using the Hasura CLI
keywords:
  - hasura
  - docs
  - CLI
  - hasura scripts
---

# Hasura CLI: hasura scripts

Execute helper scripts to manage Hasura Projects.

## Synopsis

The scripts command offers a set of helper scripts to manage a Hasura project's configuration settings. This is used when upgrading between `v1`, `v2`, or `v3` configs.

## Options

```sass
-h, --help   help for scripts
```

## Options inherited from parent commands

```sass
--envfile string      .env filename to load ENV vars from (default ".env")
--log-level string    log level (DEBUG, INFO, WARN, ERROR, FATAL) (default "INFO")
--no-color            do not colorize output (default: false)
--project string      directory where commands are executed (default: current dir)
--skip-update-check   skip automatic update check on command execution
```

## SEE ALSO

- [hasura](/hasura-cli/commands/hasura.mdx) - Hasura GraphQL Engine command line tool
- [hasura scripts update-project-v2](/hasura-cli/commands/hasura_scripts_update-project-v2.mdx) - Update the Hasura Project from config v1 to v2
- [hasura scripts update-project-v3](/hasura-cli/commands/hasura_scripts_update-project-v3.mdx) - Update the Hasura Project from config v2 to v3

_Auto generated by spf13/cobra_
