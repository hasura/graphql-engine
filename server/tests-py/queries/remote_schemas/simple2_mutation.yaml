description: Simple2 GraphQL mutation
url: /v1/graphql
status: 200
response:
  data:
    createUser:
      user:
        id: 123
        username: foobar
query:
  query: |
    mutation {
      createUser(id: 123, username: "foobar") {
        user {
          id
          username
        }
      }
    }
