type: create_remote_relationship
args:
  name: messagesNested
  table: profiles
  remote_schema: my-remote-schema
  remote_field:
    messages:
      arguments:
        where:
          id:
            xyz: "$id"
