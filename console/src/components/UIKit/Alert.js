import styled from 'styled-components';
import { layout, border, height, color, shadow, space } from 'styled-system';

export const AlertMessageBox = styled.div`
  ${layout}
  ${height}
  ${color}  
  ${border}
  ${shadow}
  ${space}
`;
