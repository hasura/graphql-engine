type: bulk
args:

- type: mssql_run_sql
  args:
    source: mssql
    sql: |
      drop table article;
      drop table author;
      drop table resident;
      drop table "user";
      drop table account;
    cascade: true
