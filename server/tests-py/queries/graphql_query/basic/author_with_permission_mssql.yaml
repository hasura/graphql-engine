description: Author can view their information only
url: /v1/graphql
status: 200
headers:
  X-Hasura-Role: user
  X-Hasura-User-Id: '2'
response:
  data:
    author:
      - id: 2
        name: Author 2

query:
  query: |
    query {
      author {
        id
        name
      }
    }
