type: create_remote_relationship
args:
  name: messageBasic
  table: profiles
  hasura_fields:
    - id
  remote_schema: my-remote-schema
  remote_field:
    message:
      arguments:
        id: "$id"
