type: run_sql
args:
  sql: |
    create table hge_tests.cohort_isolation_probe(
      id int primary key,
      val text
    );
    insert into hge_tests.cohort_isolation_probe (id, val) values
      (1, 'row-1'),
      (2, 'row-2');
