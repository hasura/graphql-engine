import styled from 'styled-components';
import {
  color,
  border,
  typography,
  layout,
  space,
  shadow
} from 'styled-system';

export const Box = styled.div`
  ${color}
  ${color}
  ${border}
  ${typography}
  ${layout}
  ${space}
  ${shadow}
`;
