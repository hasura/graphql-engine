type: update_remote_schema
args:
  name: my-remote-schema
  comment: this is from update query
  definition:
    url: "{{GRAPHQL_SERVICE_2}}"
    forward_client_headers: True
    timeout_seconds: 120
