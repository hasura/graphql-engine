type: create_remote_relationship
args:
  name: messagesReturnError
  table: profiles
  hasura_fields:
    - id
  remote_schema: my-remote-schema
  remote_field:
    errMessages:
      arguments:
          id: "$id"
