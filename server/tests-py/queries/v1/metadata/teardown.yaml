type: bulk
args:

- type: run_sql
  args:
    cascade: true
    sql: |
      drop function test_session;
      drop table article cascade;
      drop table author;
      drop table text_result cascade;
