description: Run an action with a failing handler returning 4xx status
url: /v1/graphql
status: 200
query:
  query: |
    mutation {
      client_error_action(email:"clarke@gmail.com") {
        id
        name
      }
    }

response:
  errors:
  - extensions:
      foo1: baz1
    message: something went wrong
