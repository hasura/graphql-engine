type: create_remote_to_remote_relationship
args:
  name: messageRemoteToRemote
  base_schema: my-remote-schema
  base_type: "User"
  join_fields: ["user_id"]
  remote_schema: my-remote-schema
  remote_field:
    message:
      arguments:
        id: "$user_id"
