apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: homepage
    hasuraService: custom
  name: homepage
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: homepage
  type: ClusterIP
status:
  loadBalancer: {}
