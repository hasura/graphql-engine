description: Simple2 GraphQL query
url: /v1/graphql
status: 200
response:
  data:
    user:
      username: john
      id: 2
query:
  query: |
    query {
      user(id: 2) {
        id
        username
      }
    }
