type: bulk
args:
- type: drop_derived_role
  args:
    role_name: manager_is_employee_too

- type: run_sql
  args:
    sql: |
      DROP TABLE article;
      DROP TABLE author;
      DROP TABLE "Track" cascade;
      DROP TABLE "Artist";
      DROP TABLE books;
      DROP TABLE geom_table;
      DROP TABLE jsonb_table;
      DROP TABLE gpa cascade;
      DROP TABLE auction;
      DROP TABLE team CASCADE;
      DROP TABLE manager CASCADE;
      DROP TABLE employee;
    cascade: true
