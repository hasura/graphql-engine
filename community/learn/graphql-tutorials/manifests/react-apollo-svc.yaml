apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: react-apollo
    hasuraService: custom
  name: react-apollo
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: react-apollo
  type: ClusterIP
status:
  loadBalancer: {}
