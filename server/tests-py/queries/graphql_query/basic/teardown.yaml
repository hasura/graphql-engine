type: bulk
args:

- type: run_sql
  args:
    sql: |
      drop table test_types

#Drop relationship first
- type: drop_relationship
  args:
    relationship: articles
    table:
      schema: public
      name: author
    
- type: run_sql
  args:
    sql: |
      DROP TABLE article;
      DROP TABLE author;
      DROP TABLE "user";
      DROP TYPE complex;
      DROP TYPE inventory_item;
      DROP FUNCTION full_name;
      DROP FUNCTION full_name_prefix;
      DROP TABLE person; 
    cascade: true
