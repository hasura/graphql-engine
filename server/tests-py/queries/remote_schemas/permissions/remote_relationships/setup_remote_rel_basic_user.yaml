type: create_remote_relationship
args:
  name: userBasic
  table: profiles
  hasura_fields:
    - id
  remote_schema: my-remote-schema
  remote_field:
    user:
      arguments:
        user_id: "$id"
