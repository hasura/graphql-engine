type: bulk
args:
  - type: mssql_run_sql
    args:
      source: mssql
      sql: |
        drop table spatial_types_geom

  - type: mssql_run_sql
    args:
      source: mssql
      sql: |
        drop table spatial_types_geog
