apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress
  #  annotations:
  #    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: graphql-tutorials.org
    http:
      paths:
      - path: /
        backend:
          serviceName: homepage
          servicePort: 80
      - path: /graphql/react
        backend:
          serviceName: react-apollo
          servicePort: 80
      - path: /graphql/graphiql
        backend:
          serviceName: graphiql
          servicePort: 80
      - path: /graphql
        backend:
          serviceName: hasura
          servicePort: 80