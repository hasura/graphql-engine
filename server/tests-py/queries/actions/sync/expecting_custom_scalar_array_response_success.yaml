description: custom scalar array response should be allowed
url: /v1/graphql
status: 200
query:
  query: |
    mutation {
      custom_scalar_array_response
    }
response:
  data:
    custom_scalar_array_response:
      [foo: bar]
