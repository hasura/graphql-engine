type: bulk
args:

- type: run_sql
  args:
    sql: |
      drop table author

- type: run_sql
  args:
    sql: |
      drop table address

- type: run_sql
  args:
    sql: |
      drop table resident
