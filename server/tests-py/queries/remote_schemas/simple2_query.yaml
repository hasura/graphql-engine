description: Simple2 GraphQL query
url: /v1/graphql
status: 200
response:
  data:
    user:
      id: 2
      username: john
query:
  query: |
    query {
      user(id: 2) {
        id
        username
      }
    }
