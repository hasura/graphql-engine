type: create_remote_relationship
args:
  name: message
  table: profiles
  hasura_fields:
    - id
    - name
  remote_schema: prefixer-proxy
  remote_field:
    prefixer_proxy_messages_by_pk:
      arguments:
        id: "abc"
