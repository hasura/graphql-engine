type: create_remote_relationship
args:
  name: messageBasic
  table: authors
  hasura_fields:
    - id
  remote_schema: my-remote-schema
  remote_field:
    message:
      arguments:
        id: "$id"
