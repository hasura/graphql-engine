type: bulk
args:

- type: run_sql
  args:
    sql: |
      DROP function fetch_articles(text,author);
      DROP table article;
      DROP table author;
      DROP table person;
      DROP table resident;
      drop table "user";
      drop table account;
    cascade: true
