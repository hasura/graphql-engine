type: bulk
args:

- type: run_sql
  args:
    sql: |
      CREATE TABLE order_by_nulls_test(
        id INTEGER PRIMARY KEY,
        val INTEGER
      );

      INSERT INTO order_by_nulls_test (id, val) VALUES
        (1, 3),
        (2, NULL),
        (3, 1),
        (4, NULL),
        (5, 2);

- type: track_table
  args:
    schema: public
    name: order_by_nulls_test
