description: Simple GraphQL subscription - not supported
url: /v1alpha1/graphql
status: 400
response:
  errors:
  - extensions:
      code: unexpected-payload
      path: $
    message: subscriptions are not supported over HTTP, use websockets instead
query:
  query: |
    subscription {
      hello
    }
