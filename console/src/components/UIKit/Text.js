import styled from 'styled-components';
import { fontSize, fontWeight, color, border } from 'styled-system';

export const Text = styled.p`
    ${fontSize}
    ${fontWeight}
    ${color}
    ${border}  
`;
