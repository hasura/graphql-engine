type: create_remote_relationship
args:
  name: message
  table: profiles
  hasura_fields:
    - id
    - name
  remote_schema: my-remote-schema-wrong
  remote_field:
    message:
      arguments:
        id: "$id"
