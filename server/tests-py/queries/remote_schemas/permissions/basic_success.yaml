description: Simple GraphQL query
url: /v1/graphql
status: 200
headers:
  X-Hasura-Role: user
response:
  data:
    user:
      id: 1
      username: jane
query:
  query: |
    query {
      user(id: 1) {
        id
        username
      }
    }
