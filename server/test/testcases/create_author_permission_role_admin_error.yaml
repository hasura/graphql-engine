description: Create permission with admin as role (error)
url: /v1/query
status: 400
query:
  type: create_select_permission
  args:
    table: article
    role: admin
    permission:
      columns: '*'
      filter:
        id: X-Hasura-User-Id
