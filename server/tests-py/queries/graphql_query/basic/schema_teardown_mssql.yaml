type: bulk
args:

- type: mssql_run_sql
  args:
    source: mssql
    sql: |
      drop table test_types

- type: mssql_run_sql
  args:
    source: mssql
    sql: |
      drop table article

- type: mssql_run_sql
  args:
    source: mssql
    sql: |
      drop table author

- type: mssql_run_sql
  args:
    source: mssql
    sql: |
      drop table [user]
