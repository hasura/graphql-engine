type: create_remote_relationship
args:
  name: communicationsInterface
  table: profiles
  hasura_fields:
    - id
  remote_schema: my-remote-schema
  remote_field:
    communications:
      arguments:
          id: $id
